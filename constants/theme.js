import COLORS from './colors';
import LAYOUT from './layout';

const THEME = {
  COLORS,
  LAYOUT,
  FONT: {
    SIZES: {
      small: 18,
      normal: 25,
      medium: 40,
      large: 60,
    },
    WEIGHTS: {
      normal: 400,
      semibold: 500,
      bold: 700,
    }
  },
};

export default THEME;